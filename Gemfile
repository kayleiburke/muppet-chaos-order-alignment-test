# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "pry-byebug"
gem "pry"
gem "activesupport"

gem "minitest", "~> 5.14"
gem "minitest-line"
gem "mocha", "~> 1.12"
gem "rake"
gem "fabricator"

# calculate result method into an interactor
# calculate result method - call interactor from it

# test quiz calculator
# when receive no results - empty array
# when get responses that don't correspond to choice outcomes
# properly calculates something with at least 3 questions

